+ Added ".Changed" Signals for Data ( Data.Changed:Connect(function(newValue,oldValue) end) )
+ You can now request server data from client, by using :getData(dataName,dataCategory,requestOnServer: boolean) and setting requestOnServer bool to true
+ You can now save Color3,CFrame,Vector3 without converting it to a table first :)
+ You can now choose a ban duration by using server.Moderation.banDuration (options are 1 day,1 week,1 month, 1 year - you can multiplie it) instead of using seconds for banning :)

~ Price Change from $3.99 USD to $2.99 USD
